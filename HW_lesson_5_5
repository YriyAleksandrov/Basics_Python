"""
Создать (программно) текстовый файл, записать в него программно набор чисел,
разделенных пробелами. Программа должна подсчитывать сумму чисел в файле и выводить
ее на экран.
"""
with open('user_text_5_5.txt', 'wt+', encoding='utf-8') as f:
    f.write(input('Введите ряд чисел через пробел: '))
    f.seek(0)
    for i in f:
        array = i.split()
    print(f'Сумма чисел в файле: {sum(map(float, array)):.2f}')
