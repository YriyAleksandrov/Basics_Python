"""
Создать текстовый файл (не программно), построчно записать фамилии сотрудников и
величину их окладов (не менее 10 строк). Определить, кто из сотрудников имеет оклад менее
20 тыс., вывести фамилии этих сотрудников. Выполнить подсчет средней величины дохода
сотрудников.
Пример файла:
Иванов 23543.12
Петров 13749.32
"""
with open('user_text_5_3.txt', 'rt', encoding='utf-8') as f:
    fund = 0
    for num, i in enumerate(f, 1):
        array = i.split()
        fund += float(array[1])
        if float(array[1]) < 20000:
            print(f'{array[0]} получает меньше 20 тыс.')
        else:
            continue
    print(f'Средняя величина доходда сотрудников: {fund/num}')
