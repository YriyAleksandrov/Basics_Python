"""
Необходимо создать (не программно) текстовый файл, где каждая строка описывает учебный
предмет и наличие лекционных, практических и лабораторных занятий по этому предмету и их
количество. Важно, чтобы для каждого предмета не обязательно были все типы занятий.
Сформировать словарь, содержащий название предмета и общее количество занятий по
нему. Вывести словарь на экран.
Примеры строк файла: Информатика: 100(л) 50(пр) 20(лаб).
Физика: 30(л) — 10(лаб)
Физкультура: — 30(пр) —
Пример словаря: {“Информатика”: 170, “Физика”: 40, “Физкультура”: 30}
"""
with open('user_text_5_6.txt', 'rt', encoding='utf-8') as f:
    key, val, d = [], [], {}
    for i in f:
        array = i.split()
        for x in array:
            quantity = x.split('(')
            if quantity[0] == '—':
                continue
            else:
                if len(quantity) > 1:
                    if array[0] == 'Информатика':
                        val.append(quantity[0])
                        val_i = sum(map(float, val))
                        d['Информатика'] = val_i
                    elif array[0] == 'Физика':
                        val.append(quantity[0])
                        val_i = sum(map(float, val))
                        d['Физика'] = val_i
                    elif array[0] == 'Физкультура':
                        val.append(quantity[0])
                        val_i = sum(map(float, val))
                        d['Физкультура'] = val_i
                else:
                    val.clear()
                    key.append(quantity.pop())
    print(d)
